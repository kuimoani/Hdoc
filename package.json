{
  "name": "hdoc",
  "productName": "Hdoc",
  "version": "0.1.3",
  "description": "HTML Wordprocessor",
  "author": "https://github.com/kuimoani",
  "license" : "GPL-3.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --asar --platform=win32 --arch=x64 --icon=./icons/app.ico --prune --out=dist --win32metadata.CompanyName=kuimoani --win32metadata.FileDescription=\"HTML Wordprocessor\" --win32metadata.ProductName=\"Hdoc\""
  },
  "dependencies": {
    "@exponent/electron-cookies": "^2.0.0",
    "jquery": "^3.5.0"
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-packager": "^8.7.1"
  }
}
